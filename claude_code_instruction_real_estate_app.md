# [작업지시서] 안드로이드 아파트 실거래가 조회 및 시계열 트렌드 분석 앱 개발

본 문서는 Claude Code(클로드 코드)를 통해 안드로이드(Android) 기반의 아파트 실거래가 조회 및
시계열 차트 분석 애플리케이션을 단계별로 구축하기 위한 엔지니어링 작업 지시서
(PRD & Technical Specification)입니다.

---

## 1. 프로젝트 개요 (Project Overview)

- **프로젝트 명**: RealEstate-Tracker-Android (가칭: 실거래트래커)
- **대상 플랫폼**: Android OS (최소 지원: Android 8.0 Oreo / API Level 26 이상, 권장: Android 14 / API 34 대응)
- **개발 환경 / 스택 권장**
  - 프레임워크: Flutter (Dart) 또는 Kotlin (Jetpack Compose)
  - 상태 관리: Riverpod / Bloc (Flutter) 또는 ViewModel + StateFlow (Kotlin)
  - 로컬 캐시 / DB: Room (Kotlin) 또는 Isar / Hive / SQLite (Flutter)
  - 네트워크: Retrofit / Dio + OkHttp
- **핵심 목표**
  1. 국토교통부 공공데이터포털 Open API 기반의 공신력 있는 실거래 데이터 파이프라인 구축
  2. 최근 2주간 발생한 실거래 현황 실시간 모니터링 (매매 / 전세 / 월세)
  3. 단지별 · 전용면적(평수)별 실거래가 시계열 라인 차트 인터랙션 제공
  4. 철저한 데이터 무결성 원칙 준수: **임의의 가짜 데이터(Hallucinated Data) 생성 절대 금지** 및 데이터 출처 필수 표기

---

## 2. 데이터 소스 및 무결성 관리 원칙 (Data Integrity & Source)

### 2.1 공공데이터포털 (국토교통부) Open API 규격

- **사용 API**
  1. 국토교통부_아파트매매 실거래자료 — `getRTMSDataSvcAptTradeDev`
  2. 국토교통부_아파트 전월세 자료 — `getRTMSDataSvcAptRent`
- **요청 파라미터**

  | 파라미터 | 설명 | 예시 |
  | :--- | :--- | :--- |
  | `serviceKey` | 공공데이터포털 디코딩/인코딩 인증키 | (BuildConfig 주입) |
  | `LAWD_CD` | 법정동 시군구 코드 (5자리) | `11680` (강남구) |
  | `DEAL_YMD` | 계약월 (YYYYMM) | `202609` |

- **네이버부동산 연동 관련 주의사항**
  - 네이버부동산 비공식 스크래핑 시 WAF 차단 위험이 있으므로, 기준 데이터는 **100% 국토교통부
    공공데이터포털 API**를 기반으로 처리하고, 네이버부동산은 단지 페이지 **웹뷰 딥링크** 형태로만 연계할 것.

### 2.2 엄격한 데이터 검증 및 무결성 원칙

1. **가짜 가격 생성 금지**: 데이터가 없거나 수신 실패 시 임의의 추정치를 넣지 말고
   "거래 데이터 없음", "국토교통부 미신고 건"으로 명시한다.
2. **출처 표기 의무화**: 모든 카드 및 단지 상세 화면에
   `[데이터 출처: 국토교통부 실거래가 공개시스템 (기준일시: YYYY-MM-DD hh:mm)]`을 명시한다.
3. **법정 계약 취소 건 필터링**: `해제여부` 필드를 확인하여 취소된 계약은 취소선 처리하거나
   목록에서 제외하는 옵션을 제공한다.

---

## 3. 대상 지역 정의 (Target Regions & Lawd Codes)

| 시/도 | 시/구 단위 | 관할 법정동 코드(LAWD_CD) | 비고 |
| :--- | :--- | :--- | :--- |
| 서울특별시 | 25개 자치구 전역 | 11110 ~ 11740 | 강남, 서초, 송파, 마포 등 25개 구 전체 |
| 성남시 | 분당구 / 수정구 / 중원구 | 41135 / 41131 / 41133 | 판교, 서현, 야탑, 위례 등 |
| 용인시 | 수지구 / 기흥구 / 처인구 | 41465 / 41463 / 41461 | 풍덕천, 상현, 보정, 신갈 등 |
| 수원시 | 영통구 / 팔달구 / 권선구 / 장안구 | 41117 / 41115 / 41113 / 41111 | 광교, 매탄, 망포, 인계 등 |
| 화성시 (동탄) | 동탄출장소 관할 | 41590 (동탄 관할 동 필터링) | 청계, 목, 영천, 산척, 송, 장지, 오산, 신, 반송, 석우, 능동 |

> 화성시는 `LAWD_CD` 단위가 시(41590) 전체이므로, API 응답의 법정동(`umdNm`) 값을 기준으로
> 동탄 관할 동만 클라이언트에서 필터링한다.

---

## 4. 핵심 기능 요구사항 (Functional Requirements)

### 4.1 기본 메인 화면: 최근 2주간 실거래 모니터링

1. **타임프레임**: 오늘(D) 기준 `D-14` 부터 `D` 까지 계약일자 필터링.
2. **3-Way 탭**: `[매매]` / `[전세]` / `[월세]` 전환 (월세는 보증금 / 월세액 분리 표기).
3. **목록 카드 UI**: 단지명, 지역/동, 전용면적(m² 및 평형 병기: `평 = m² × 0.3025`), 층수,
   계약일, 실거래가(억 / 만원 포맷), 직전 대비 등락률.
4. **필터 및 정렬**: 지역 필터, 평형대 필터(소형 / 중형 / 대형), 최신 거래순 / 금액순.

### 4.2 상세 화면: 실거래가 추이 라인 차트

1. **전용면적 선택 칩**: 단지 내 평형 선택 칩(Chip) 버튼.
2. **시계열 라인 차트**: 6개월 / 1년 / 3년 / 전체 기간, 매매·전세 동시 비교,
   데이터 포인트 터치 툴팁, 신고가 가이드라인.
3. **거래 이력 리스트**: 차트 하단에 전체 실거래 일자별 내역 테이블.

---

## 5. UI/UX 및 디자인 명세

1. **한글 폰트**: 글자 깨짐 방지를 위해 **Pretendard** 폰트 번들링 강제 적용.
2. **화폐 포맷**: 한국 정서에 맞게 억 / 만원 단위 변환 (예: `8억 7,500만원`).
3. **컬러**
   - Deep Royal Blue `#1E40AF` — Primary
   - Carmine Red `#DC2626` — 신고가 / 상승
   - Cobalt Blue `#2563EB` — 하락
4. **UX**: Pull-to-refresh 및 Skeleton Shimmer 로딩 UI, 모던 미니멀리즘 디자인.

---

## 6. 개발 단계별 구현 로드맵

| 단계 | 내용 |
| :--- | :--- |
| **Step 1** | 프로젝트 셋업, Pretendard 폰트 설정, 화폐/면적 변환 유틸리티, 법정동 코드 테이블 구축 |
| **Step 2** | 공공데이터포털 실거래가 API 클라이언트 및 최근 2주 필터 / 로컬 캐시 구현 |
| **Step 3** | 메인 화면 (최근 2주 실거래 피드, 3-Way 탭, 지역 필터) UI 개발 |
| **Step 4** | 상세 화면 (평형 선택 칩, 인터랙티브 시계열 라인 차트) 개발 |
| **Step 5** | 무결성 검증 (Mock 데이터 완전 배제 확인, 출처 라벨 표기 확인) |
